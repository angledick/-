# ====== 任务-Worker 绑定配置文件 ======
#
# 仅保留三项核心定时任务的 Worker 绑定。
#
# ========================================
bindings:
  scan_regulation_changes:
    worker_code: regulation_worker
    enabled: true
  heartbeat_check:
    worker_code: system_worker
    enabled: true
  shopify_product_sync:
    worker_code: listing_worker
    enabled: true
