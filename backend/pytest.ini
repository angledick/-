[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    live: 需要运行中的后端服务器(localhost:8002)的全流程测试
    slow: 执行时间较长的测试
filterwarnings =
    ignore:unclosed database:ResourceWarning
    ignore:Direct api_key:DeprecationWarning
    ignore:legacy embedding:DeprecationWarning
