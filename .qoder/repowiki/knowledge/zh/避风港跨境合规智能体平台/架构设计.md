- 前后端分离：前端 (Vite/React) 通过 REST API 与后端 (FastAPI) 交互，并建立 WebSocket 连接接收实时推送。
- 统一入口编排：后端 `main.py` 集中注册 20+ 业务路由模块并管理全局生命周期（调度器、事件总线、Agent 初始化）。
- 状态同步机制：前端利用 Context API (Auth, WebSocket, Notification) 维持全局会话状态，确保多页面间的操作连贯性。
- 配置驱动扩展：系统核心逻辑（如 Agent 行为、技能注册、OAuth 连接）由后端存储层统一管理，前端通过配置中心动态渲染。