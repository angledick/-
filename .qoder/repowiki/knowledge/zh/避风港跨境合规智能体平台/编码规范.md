- 后端采用分层架构设计，严格区分 API 路由层、核心引擎层 (core)、业务服务层 (services) 与存储层 (storage)。
- 前端遵循 Utility-first CSS 规范，全面使用 TailwindCSS 进行样式开发，并采用函数式组件配合 TypeScript 严格模式。
- 跨模块通信采用统一的 API 客户端封装 (frontend/src/api/config.ts)，确保类型安全与请求拦截的一致性。