# 前端变更路线图 · 体验重设计 v2

## 核心设计理念转变

**现有问题**：
1. 无流式对话能力，用户无法完成实时交互
2. 无工具/技能/Agent配置切换界面，用户无法通过对话配置对接系统
3. 业务合规流未区分系统级与产品级
4. 对话不展示Skill事件/思考过程/执行计划，用户看不到AI在做什么

**新设计原则**：
1. **流式对话是一切的基础** — SSE实时推送token + skill事件 + 思考过程 + 执行计划，所有业务操作最终通过对话完成
2. **系统合规 vs 产品合规双层架构** — 系统合规：全局展示；产品合规：产品卡片，点开即对话
3. **工具/技能/Agent可配置可切换** — 用户可选择工具集、技能、Agent，通过对话配置对接系统
4. **对话即操作台** — 对话不只是问答，而是展示Skill调用、思考链、Workflow步骤、操作建议的完整操作台
5. **事件驱动的主动体验** — 系统主动推送合规事件和操作建议，用户确认即执行

---

## 新页面架构

```
侧边栏导航:
├── 概览 (Overview)
│   └── 全局合规健康度 + 待办事项 + 近期事件流 + 快速入口
│
├── 系统合规 (System Compliance) — 全局展示
│   └── 10阶段合规总览：每阶段全局合规通过率、风险产品数、待办数
│   └── 全局法规变更/认证到期/指标预警 汇总
│   └── 点击任意阶段 → 展开全局检查清单 + 关联产品列表
│
├── 产品合规 (Product Compliance) — 产品卡片 + 点开即对话
│   ├── 产品卡片网格（按生命周期分组：概念/设计/采购/就绪/在售/履约/售后/下架）
│   │   └── 每张卡片：产品名+目标市场+合规状态+健康度+认证状态
│   │   └── 点击卡片 → 进入产品专属流式对话界面
│   │
│   └── 产品对话界面（核心交互）：
│       ├── 左侧：产品信息面板（HS编码/认证/供应商/指标/事件时间线）
│       ├── 中间：流式对话区（SSE实时推送）
│       │   ├── 对话消息（用户输入 + AI回复，逐token流式输出）
│       │   ├── Skill执行事件块（高亮显示：调用了什么Skill、入参、返回结果）
│       │   ├── 思考过程块（Agent推理链、决策依据，可折叠）
│       │   ├── 执行计划块（Workflow步骤列表 + 当前步骤进度条）
│       │   └── 操作建议卡片（确认/修改/执行/跳过）
│       └── 右侧：合规面板（检查结果/风险等级/整改项/快捷操作）
│
├── 对话工作台 (Chat Workspace) — 独立对话入口
│   └── 流式对话界面（不绑定产品，用于通用问答/配置/系统管理）
│   └── 顶部：工具/技能/Agent 选择器与配置切换栏
│   │   ├── Agent选择器：Manager / QAAgent / Worker(合规) / Worker(认证) / 自定义
│   │   ├── 工具面板：已启用工具列表（MCP工具/Shopify API/外部API），可开关
│   │   └── 技能面板：已安装Skills列表，可选择激活/停用
│   └── 对话中展示：Skill事件/思考过程/执行计划/操作建议
│   └── 支持通过对话配置系统（如"帮我配置Shopify集成"→ 对话引导OAuth流程）
│
├── 合规知识库 (Knowledge)
│   └── 法规/认证/HS编码/市场要求，按市场×品类组织
│
└── 系统管理 (System)
    ├── Agent配置（Agent列表/角色/工具集/Skills/权限）
    ├── 集成管理（Shopify OAuth/飞书/钉钉连接状态）
    ├── 通知规则（事件→通知→通道→接收人配置）
    ├── 记忆树浏览器（Obsidian Wiki风格）
    └── 用户与权限管理
```

---

## 流式对话核心设计

### SSE消息协议

对话通过SSE（Server-Sent Events）流式推送，每条消息携带类型标识：

```typescript
// SSE消息类型定义
type StreamEvent =
  | { type: 'token'; content: string }                    // AI回复token逐字输出
  | { type: 'skill_start'; skill: string; args: object }  // Skill开始执行
  | { type: 'skill_end'; skill: string; result: object }  // Skill执行完成
  | { type: 'thinking'; content: string }                  // Agent思考过程
  | { type: 'plan'; steps: PlanStep[]; current: number }   // 执行计划+当前步骤
  | { type: 'action_card'; actions: Action[] }             // 操作建议卡片
  | { type: 'done' }                                       // 对话轮次结束
```

### 流式对话组件渲染规则

```typescript
// 消息区渲染：根据type渲染不同UI块
const StreamMessageRenderer: React.FC<{ events: StreamEvent[] }> = ({ events }) => {
  return (
    <div className="stream-container">
      {events.map((event, i) => {
        switch (event.type) {
          case 'token':
            return <span key={i} className="ai-token">{event.content}</span>
          case 'skill_start':
            return <SkillEventBlock key={i} status="running" skill={event.skill} args={event.args} />
          case 'skill_end':
            return <SkillEventBlock key={i} status="done" skill={event.skill} result={event.result} />
          case 'thinking':
            return <ThinkingBlock key={i} content={event.content} collapsible />
          case 'plan':
            return <PlanBlock key={i} steps={event.steps} current={event.current} />
          case 'action_card':
            return <ActionCardGroup key={i} actions={event.actions} />
        }
      })}
    </div>
  )
}
```

### Skill执行事件块设计

```
┌─────────────────────────────────────────────────┐
│ 🔧 shopify-admin                                │
│ 查询产品 p_led_de_001 的 Metafields             │
│ ─────────────────────────────────────────────── │
│ 入参: { product_id: "p_led_de_001", ... }       │
│ 结果: HS编码=8541.4100, CE认证=valid             │
│ ⏱ 1.2s ✅ 完成                                  │
└─────────────────────────────────────────────────┘
```

### 思考过程块设计

```
┌─────────────────────────────────────────────────┐
│ 💭 思考过程                          [展开/折叠] │
│ ─────────────────────────────────────────────── │
│ 产品LED灯带-德国的WEEE认证即将到期，             │
│ 需要确认续期流程。根据合规规则，WEEE认证          │
│ 到期后产品将被限制销售，建议立即续期。            │
│ 调用shopify-custom-data查询当前认证状态...       │
└─────────────────────────────────────────────────┘
```

### 执行计划块设计

```
┌─────────────────────────────────────────────────┐
│ 📋 执行计划: WEEE认证续期                 2/4步  │
│ ─────────────────────────────────────────────── │
│ ✅ Step 1: 查询当前认证状态                      │
│ ✅ Step 2: 更新Metafield weee_expiry             │
│ 🔄 Step 3: 更新产品标签 weee-renewed-2027       │
│ ⏳ Step 4: 触发合规重检                          │
└─────────────────────────────────────────────────┘
```

---

## 工具/技能/Agent配置切换界面

### Agent选择器

```
┌─────────────────────────────────────────────┐
│ Agent: [Manager Agent ▼]                    │
│                                              │
│ 可选Agent:                                   │
│  ● Manager Agent — 协调者，任务拆解与分配     │
│  ○ QAAgent — 系统问答与配置管理              │
│  ○ Compliance Worker — 合规检查执行          │
│  ○ Certification Worker — 认证管理           │
│  ○ 自定义Agent...                            │
└─────────────────────────────────────────────┘
```

### 工具面板

```
┌─────────────────────────────────────────────┐
│ 🔧 工具 (7/12 已启用)              [管理]   │
│ ─────────────────────────────────────────── │
│ ✅ shopify-admin         Shopify Admin API  │
│ ✅ shopify-custom-data   Metafields管理     │
│ ✅ shopify-functions     自定义Function      │
│ ✅ rule_engine           合规规则引擎        │
│ ✅ knowledge_search      知识库检索          │
│ ✅ event_bus             事件总线查询        │
│ ✅ risk_alert            风险预警            │
│ ⬚ shopify-storefront   前端Storefront API  │
│ ⬚ shopify-liquid        Liquid模板          │
│ ...                                         │
└─────────────────────────────────────────────┘
```

### 技能面板

```
┌─────────────────────────────────────────────┐
│ 🎯 技能 (5/15 已激活)              [管理]   │
│ ─────────────────────────────────────────── │
│ ✅ shopify-admin         Admin GraphQL操作  │
│ ✅ shopify-custom-data   Metafields/Metaobj │
│ ✅ compliance-checker    合规检查           │
│ ✅ cert-manager          认证管理           │
│ ✅ market-monitor        市场监控           │
│ ⬚ shopify-onboarding   店铺搭建引导       │
│ ⬚ skill-vetter         技能安全审查       │
│ ...                                         │
└─────────────────────────────────────────────┘
```

### 对话配置对接系统示例

```
用户: "帮我配置Shopify集成"

AI (流式输出):
  💭 思考: 用户需要配置Shopify集成，需要引导OAuth流程...
  
  📋 执行计划: Shopify集成配置
  ⏳ Step 1: 检查现有配置状态
  ⏳ Step 2: 引导OAuth授权
  ⏳ Step 3: 配置Webhook
  ⏳ Step 4: 测试连接

  🔧 rule_engine: 检查现有Shopify配置
  → 未找到现有Shopify配置

  好的，我来帮你配置Shopify集成。请提供以下信息：

  ┌─────────────────────────────────────────┐
  │ 🔗 Shopify集成配置                       │
  │                                          │
  │ Shopify商店域名: [your-store.myshopify.com] │
  │ API访问令牌:     [••••••••••••••••]       │
  │                                          │
  │ [开始OAuth授权]  [手动输入凭证]            │
  └─────────────────────────────────────────┘
```

---

## 分阶段实施路线

### Phase 1: 流式对话核心 + 系统合规 + 产品卡片 + 布局骨架（4-6周）

**目标**：建立流式对话基础能力，实现系统合规全局展示和产品合规卡片入口，用户可完成基本的流式对话交互。

#### 1.1 流式对话核心（最高优先级）

| 变更项 | 现有组件 | 目标状态 | 工作量 |
|--------|----------|----------|--------|
| **SSE流式对话引擎** | 无 | 建立SSE连接管理，支持token/skill/thinking/plan/action_card事件流 | 1.5周 |
| **流式消息渲染器** | `MessageBubble.tsx` | 根据事件类型渲染不同UI块：token文本/Skill事件块/思考块/计划块/操作卡片 | 1周 |
| **对话输入组件** | `ChatPanel.tsx` | 升级：支持发送消息、显示流式响应、操作卡片交互 | 0.5周 |
| **Skill事件块组件** | 无 | 展示Skill调用状态（运行中/完成/失败）、入参、返回结果、耗时 | 0.5周 |
| **思考过程块组件** | 无 | 可折叠展示Agent推理过程，支持Markdown渲染 | 0.5周 |
| **执行计划块组件** | 无 | 展示Workflow步骤列表、当前进度、步骤状态 | 0.5周 |
| **操作建议卡片组件** | 无 | 可交互卡片：确认/修改/执行/跳过，支持参数编辑 | 0.5周 |

#### 1.2 工具/技能/Agent配置切换

| 变更项 | 现有组件 | 目标状态 | 工作量 |
|--------|----------|----------|--------|
| **Agent选择器** | 无 | 下拉选择器：Manager/QAAgent/Worker(合规)/Worker(认证)/自定义 | 0.5周 |
| **工具面板** | 无 | 已启用工具列表，可开关切换，展示工具描述 | 0.5周 |
| **技能面板** | 无 | 已安装Skills列表，可激活/停用，展示技能描述 | 0.5周 |
| **配置切换状态管理** | 无 | 全局Context管理当前Agent/工具集/技能集状态 | 0.5周 |

#### 1.3 布局与导航重构

| 变更项 | 现有组件 | 目标状态 | 工作量 |
|--------|----------|----------|--------|
| **侧边栏重构** | `Sidebar.tsx` | 概览/系统合规/产品合规/对话工作台/知识库/系统管理 | 0.5周 |
| **全局布局** | 无 | `Layout.tsx`：侧边栏 + 主内容区 | 0.5周 |
| **路由系统** | `App.tsx` | 嵌套路由：`/compliance/system`、`/products/:id/chat`、`/chat` | 0.5周 |

#### 1.4 系统合规页面（全局展示）

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **10阶段合规总览** | 横向时间轴，每阶段显示：合规通过率、风险产品数、待办数 | 1周 |
| **阶段详情展开** | 点击阶段展开：全局检查清单（通过/未通过/待检查）、关联产品列表 | 0.5周 |
| **全局合规预警汇总** | 法规变更/认证到期/指标预警的全局汇总列表 | 0.5周 |

#### 1.5 产品合规页面（产品卡片 + 点开即对话）

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **产品卡片网格** | 卡片展示：产品名+目标市场+合规状态+健康度+认证状态，按生命周期分组 | 1周 |
| **产品对话界面** | 三栏布局：左(产品信息) + 中(流式对话) + 右(合规面板) | 1周 |
| **产品信息面板** | HS编码/认证状态/供应商/指标/事件时间线 | 0.5周 |
| **新增产品向导** | 通过对话引导添加产品：产品名→目标市场→HS编码→触发初始合规检查 | 0.5周 |

#### 1.6 对话工作台页面

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **独立对话界面** | 不绑定产品的通用对话界面，顶部展示Agent/工具/技能选择器 | 0.5周 |
| **配置对话能力** | 支持通过对话配置对接系统（如"配置Shopify集成"→对话引导OAuth） | 1周 |

#### 1.7 WebSocket事件基础

| 变更项 | 现有组件 | 目标状态 | 工作量 |
|--------|----------|----------|--------|
| **WebSocket管理器** | `useWebSocket` hook | 升级为全局WebSocketContext，支持重连/心跳/消息路由 | 0.5周 |
| **事件推送** | `RiskCenter.tsx` | 扩展为全局事件推送 | 0.5周 |

**Phase 1 新增文件**：
| 文件路径 | 说明 |
|----------|------|
| `frontend/src/components/Layout.tsx` | 全局布局 |
| `frontend/src/components/StreamChat.tsx` | 流式对话核心组件（SSE连接+消息渲染） |
| `frontend/src/components/StreamMessageRenderer.tsx` | 流式消息渲染器（按type分发） |
| `frontend/src/components/SkillEventBlock.tsx` | Skill执行事件块 |
| `frontend/src/components/ThinkingBlock.tsx` | 思考过程块 |
| `frontend/src/components/PlanBlock.tsx` | 执行计划块 |
| `frontend/src/components/ActionSuggestionCard.tsx` | 操作建议卡片 |
| `frontend/src/components/AgentSelector.tsx` | Agent选择器 |
| `frontend/src/components/ToolPanel.tsx` | 工具面板 |
| `frontend/src/components/SkillPanel.tsx` | 技能面板 |
| `frontend/src/components/PipelineNav.tsx` | 10阶段业务流程导航 |
| `frontend/src/components/ComplianceCheckCard.tsx` | 合规检查卡片 |
| `frontend/src/components/ProductCard.tsx` | 产品卡片组件 |
| `frontend/src/components/EventTimeline.tsx` | 事件时间线 |
| `frontend/src/pages/OverviewPage.tsx` | 概览仪表盘 |
| `frontend/src/pages/SystemCompliancePage.tsx` | 系统合规页面（全局展示） |
| `frontend/src/pages/ProductListPage.tsx` | 产品合规页面（卡片网格） |
| `frontend/src/pages/ProductChatPage.tsx` | 产品对话界面（三栏布局） |
| `frontend/src/pages/ChatWorkspacePage.tsx` | 对话工作台（独立对话+配置切换） |
| `frontend/src/context/WebSocketContext.tsx` | 全局WebSocket上下文 |
| `frontend/src/context/AgentConfigContext.tsx` | Agent/工具/技能配置状态 |

**Phase 1 修改文件**：
| 文件路径 | 变更说明 |
|----------|----------|
| `frontend/src/App.tsx` | 路由系统升级 + Layout集成 |
| `frontend/src/components/Sidebar.tsx` | 重构导航 |
| `frontend/src/components/ChatPanel.tsx` | 重构为SSE流式对话 |
| `frontend/src/components/MessageBubble.tsx` | 支持结构化消息块 |
| `frontend/src/pages/CompliancePage.tsx` | 重构集成到系统合规 |
| `frontend/src/hooks/useWebSocket.ts` | 升级为WebSocketContext |

**Phase 1 后端API需求**：
| API | 方法 | 说明 |
|-----|------|------|
| `/api/v1/chat/stream` | SSE | 流式对话接口（核心），推送token/skill/thinking/plan事件 |
| `/api/v1/chat/config` | GET/PUT | 获取/更新对话配置（当前Agent/工具集/技能集） |
| `/api/v1/agents` | GET | 获取可用Agent列表 |
| `/api/v1/tools` | GET | 获取可用工具列表 |
| `/api/v1/skills` | GET | 获取已安装Skills列表 |
| `/api/v1/pipeline/stages` | GET | 10阶段合规状态 |
| `/api/v1/products` | GET | 产品列表 |
| `/api/v1/products/:id` | GET | 产品详情 |

---

### Phase 2: 主动告知 + 操作执行闭环 + 事件驱动（3-4周）

**目标**：系统主动推送合规事件，用户从通知一键进入对话处理，操作执行全程可视化。

#### 2.1 主动告知系统

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **通知中心** | 全局通知中心：按优先级排序的合规事件列表，点击进入产品对话 | 1周 |
| **Toast通知** | WebSocket实时推送弹窗（认证到期/法规变更/合规失败） | 0.5周 |
| **每日合规简报** | 概览页顶部展示当日简报（新增预警/待办/AI洞察） | 0.5周 |

#### 2.2 操作执行闭环

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **操作确认流** | 操作建议卡片确认后，对话区自动展示执行计划+进度 | 0.5周 |
| **结果回写展示** | 执行完成后，对话区展示影响范围（更新了哪些数据/指标/事件） | 0.5周 |
| **产品面板联动** | 操作完成后，产品信息面板和事件时间线自动刷新 | 0.5周 |

#### 2.3 产品详情增强

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **指标看板Tab** | 产品对话界面右侧增加指标看板：健康度/认证到期/退货率/拒付率 | 0.5周 |
| **事件时间线增强** | 时间线支持过滤（按事件类型）、搜索、导出 | 0.5周 |

**Phase 2 新增文件**：
| 文件路径 | 说明 |
|----------|------|
| `frontend/src/components/NotificationCenter.tsx` | 通知中心组件 |
| `frontend/src/components/ToastNotification.tsx` | Toast通知组件 |
| `frontend/src/components/DailyBrief.tsx` | 每日合规简报 |
| `frontend/src/components/ExecutionResult.tsx` | 执行结果展示 |
| `frontend/src/context/NotificationContext.tsx` | 通知状态管理 |

---

### Phase 3: 记忆树 + 指标监控 + Agent监控（3-4周）

**目标**：提供深层数据洞察能力。

#### 3.1 记忆树浏览器

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **记忆树导航** | 按市场/产品/法规组织的目录树 + 面包屑导航 | 1周 |
| **Markdown渲染** | 支持渲染、代码高亮、双向链接 | 0.5周 |
| **全文搜索** | 全文搜索 + 语义搜索 | 0.5周 |

#### 3.2 指标监控

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **全局指标仪表盘** | 系统健康度、合规通过率、认证到期分布趋势图 | 1周 |
| **指标预警配置** | 自定义指标阈值、预警通道 | 0.5周 |

#### 3.3 Agent状态监控

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **Agent状态面板** | Manager/Worker状态、任务进度、通信日志 | 0.5周 |
| **Agent配置管理** | Agent列表/角色/工具集/Skills/权限配置界面 | 1周 |

**Phase 3 新增文件**：
| 文件路径 | 说明 |
|----------|------|
| `frontend/src/pages/MemoryTreePage.tsx` | 记忆树浏览器 |
| `frontend/src/pages/MetricsPage.tsx` | 指标监控中心 |
| `frontend/src/pages/AgentMonitorPage.tsx` | Agent监控与配置 |
| `frontend/src/components/memory/TreeView.tsx` | 树形视图 |
| `frontend/src/components/memory/MarkdownViewer.tsx` | Markdown渲染 |
| `frontend/src/components/metrics/MetricCard.tsx` | 指标卡片 |
| `frontend/src/components/metrics/TrendChart.tsx` | 趋势图表 |

---

### Phase 4: 多频道 + 报表 + 集成管理 + 权限（3-4周）

**目标**：扩展到多平台协同。

#### 4.1 多频道适配

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **频道适配器框架** | 统一接口（renderMessage/renderNotification/handleCallback） | 1周 |
| **飞书/钉钉适配器** | 消息卡片渲染、操作按钮 | 1周 |

#### 4.2 报表与集成

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **合规报表** | 按产品/市场/时间段生成，PDF/Excel导出 | 1周 |
| **集成管理页面** | Shopify OAuth/飞书/钉钉连接状态、一键接入 | 1周 |
| **通知规则配置** | 事件→通知→通道→接收人 可视化配置 | 0.5周 |

#### 4.3 权限管理

| 变更项 | 目标状态 | 工作量 |
|--------|----------|--------|
| **多角色管理** | 超级管理员/运营管理员/合规审查员/商品运营/只读 | 0.5周 |
| **审批流配置** | 操作审批规则、审批人、超时策略 | 0.5周 |

---

## 技术选型

| 库 | 用途 | 版本 |
|----|------|------|
| `react-router-dom` | 嵌套路由 | v7 |
| `tailwindcss` | 统一样式方案 | v4 |
| `recharts` | 数据可视化（指标趋势图） | v2 |
| `react-markdown` | Markdown渲染 | v9 |
| `framer-motion` | 动画（通知弹窗/页面过渡） | v11 |
| `zustand` | 轻量状态管理 | v5 |
| **EventSource (原生)** | SSE流式对话 | 浏览器内置 |

> 注：SSE使用浏览器原生EventSource API，无需额外依赖。如需支持POST请求的SSE，使用fetch + ReadableStream。

---

## 前后端联动点

### 数据流

| 用户场景 | 后端提供 | 前端展示 | 联动方式 |
|----------|----------|----------|----------|
| **流式对话** | SSE `/api/v1/chat/stream` | 逐token输出 + Skill事件 + 思考 + 计划 | SSE |
| **Agent/工具/技能列表** | GET `/api/v1/agents` `/tools` `/skills` | 选择器/面板 | REST API |
| **对话配置** | GET/PUT `/api/v1/chat/config` | Agent/工具/技能切换 | REST API |
| **系统合规状态** | GET `/api/v1/pipeline/stages` | 10阶段时间轴 | REST API |
| **产品列表** | GET `/api/v1/products` | 产品卡片网格 | REST API |
| **产品详情** | GET `/api/v1/products/:id` | 产品信息面板 | REST API |
| **产品事件** | GET `/api/v1/products/:id/events` | 事件时间线 | REST + WS |
| **产品指标** | GET `/api/v1/products/:id/metrics` | 指标看板 | REST API |

### 事件推送 (WebSocket)

| 事件类型 | 前端响应 |
|----------|----------|
| `product:created` | 产品卡片网格刷新 |
| `compliance:check_failed` | 通知弹窗，点击进入产品对话 |
| `certification:expiring` | 高优先级通知 + 推荐操作 |
| `regulation:updated` | 概览页简报更新 |
| `agent:task_update` | 对话区执行计划块更新 |

---

## 验收标准

### Phase 1 验收
- [ ] 流式对话：SSE连接正常，AI回复逐token输出，无闪烁
- [ ] Skill事件块：展示Skill调用状态（运行中/完成/失败）、入参、结果、耗时
- [ ] 思考过程块：可折叠展示Agent推理过程
- [ ] 执行计划块：展示步骤列表、当前进度、步骤状态
- [ ] 操作建议卡片：可交互（确认/修改/执行/跳过）
- [ ] Agent选择器：可切换不同Agent
- [ ] 工具面板：显示已启用工具列表，可开关
- [ ] 技能面板：显示已安装Skills，可激活/停用
- [ ] 系统合规页面：10阶段全局合规总览，点击展开阶段详情
- [ ] 产品合规页面：产品卡片网格，按生命周期分组
- [ ] 产品对话界面：点击产品卡片进入三栏对话界面，流式对话正常
- [ ] 对话工作台：独立对话界面 + 顶部配置切换栏，可通过对话配置对接系统

### Phase 2 验收
- [ ] 通知中心：按优先级排序的合规事件，点击进入产品对话
- [ ] Toast通知：WebSocket实时推送弹窗
- [ ] 操作执行闭环：确认操作后展示执行计划+进度+结果
- [ ] 产品面板联动：操作完成后自动刷新

### Phase 3 验收
- [ ] 记忆树浏览器支持Obsidian Wiki风格浏览
- [ ] 指标监控中心展示全局和产品级指标趋势
- [ ] Agent状态监控面板实时显示Agent状态
- [ ] Agent配置管理界面支持工具集/Skills配置

### Phase 4 验收
- [ ] 飞书/钉钉消息卡片正常渲染
- [ ] 合规报表支持PDF/Excel导出
- [ ] 集成管理页面展示OAuth连接状态
- [ ] 权限管理支持多角色配置

---

## 文件清单总览

### 新增（约28个文件）

| 文件路径 | 说明 | Phase |
|----------|------|-------|
| `frontend/src/components/Layout.tsx` | 全局布局 | 1 |
| `frontend/src/components/StreamChat.tsx` | 流式对话核心（SSE连接+事件分发） | 1 |
| `frontend/src/components/StreamMessageRenderer.tsx` | 流式消息渲染器 | 1 |
| `frontend/src/components/SkillEventBlock.tsx` | Skill执行事件块 | 1 |
| `frontend/src/components/ThinkingBlock.tsx` | 思考过程块 | 1 |
| `frontend/src/components/PlanBlock.tsx` | 执行计划块 | 1 |
| `frontend/src/components/ActionSuggestionCard.tsx` | 操作建议卡片 | 1 |
| `frontend/src/components/AgentSelector.tsx` | Agent选择器 | 1 |
| `frontend/src/components/ToolPanel.tsx` | 工具面板 | 1 |
| `frontend/src/components/SkillPanel.tsx` | 技能面板 | 1 |
| `frontend/src/components/PipelineNav.tsx` | 10阶段导航 | 1 |
| `frontend/src/components/ComplianceCheckCard.tsx` | 合规检查卡片 | 1 |
| `frontend/src/components/ProductCard.tsx` | 产品卡片 | 1 |
| `frontend/src/components/EventTimeline.tsx` | 事件时间线 | 1 |
| `frontend/src/pages/OverviewPage.tsx` | 概览仪表盘 | 1 |
| `frontend/src/pages/SystemCompliancePage.tsx` | 系统合规（全局） | 1 |
| `frontend/src/pages/ProductListPage.tsx` | 产品合规（卡片） | 1 |
| `frontend/src/pages/ProductChatPage.tsx` | 产品对话界面 | 1 |
| `frontend/src/pages/ChatWorkspacePage.tsx` | 对话工作台 | 1 |
| `frontend/src/context/WebSocketContext.tsx` | WebSocket上下文 | 1 |
| `frontend/src/context/AgentConfigContext.tsx` | Agent/工具/技能配置 | 1 |
| `frontend/src/components/NotificationCenter.tsx` | 通知中心 | 2 |
| `frontend/src/components/ToastNotification.tsx` | Toast通知 | 2 |
| `frontend/src/components/DailyBrief.tsx` | 每日合规简报 | 2 |
| `frontend/src/pages/MemoryTreePage.tsx` | 记忆树浏览器 | 3 |
| `frontend/src/pages/MetricsPage.tsx` | 指标监控中心 | 3 |
| `frontend/src/pages/AgentMonitorPage.tsx` | Agent监控与配置 | 3 |
| `frontend/src/pages/IntegrationPage.tsx` | 集成管理 | 4 |

### 修改（约8个文件）

| 文件路径 | 变更说明 | Phase |
|----------|----------|-------|
| `frontend/src/App.tsx` | 路由系统升级 + Layout | 1 |
| `frontend/src/components/Sidebar.tsx` | 重构导航 | 1 |
| `frontend/src/components/ChatPanel.tsx` | 重构为SSE流式对话 | 1 |
| `frontend/src/components/MessageBubble.tsx` | 支持结构化消息块 | 1 |
| `frontend/src/pages/CompliancePage.tsx` | 集成到系统合规 | 1 |
| `frontend/src/hooks/useWebSocket.ts` | 升级为WebSocketContext | 1 |
| `frontend/src/pages/KnowledgePage.tsx` | 集成记忆树 | 3 |
| `frontend/src/pages/UserManagePage.tsx` | 多角色权限 | 4 |